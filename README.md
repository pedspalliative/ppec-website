# pedspalliative.org

Website for Pediatric Palliative Education Consultants (PPEC).

Static site served via GitHub Pages. The five pages (`index`, `about`, `services`, `resources`, `contact`) are plain HTML with shared styles in `css/` and behavior in `js/`.
